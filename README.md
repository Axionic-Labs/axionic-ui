# @axionic/ui

Shared React component library for Axionic Labs web applications. Built on **Panda CSS** + **Park UI** (Ark UI primitives) with a branded design token system.

## Install

This package is installed as a GitHub git dependency:

```json
{
  "dependencies": {
    "@axionic/ui": "Axionic-Labs/axionic-ui"
  }
}
```

**Peer dependencies:** `react` (18 or 19), `react-dom` (18 or 19). Consumers must have `@pandacss/dev` (^1.8) in their own devDependencies to run Panda CSS codegen.

## Setup

### 1. Configure Panda CSS

In your `panda.config.ts`, use the Axionic preset:

```ts
import { defineConfig } from '@pandacss/dev';
import { createAxionicPreset } from '@axionic/ui/preset';

export default defineConfig({
  presets: [createAxionicPreset()],
  include: ['./src/**/*.{ts,tsx}', './node_modules/@axionic/ui/dist/**/*.js'],
  outdir: 'styled-system',
  jsxFramework: 'react',
});
```

Run `panda codegen` to generate the `styled-system/` directory.

### 2. Load the library fonts

The preset expects:

- `Graphik` for body/UI text
- `Satoshi` for display/headings
- `JetBrains Mono` for code

Self-host those fonts in your app, or map them to equivalent family names in your global CSS before rendering shared components.

Example:

```css
@font-face {
  font-family: 'Graphik';
  src: url('/fonts/Graphik-Regular.woff2') format('woff2');
}

@font-face {
  font-family: 'Satoshi';
  src: url('/fonts/Satoshi-Variable.woff2') format('woff2');
}

@font-face {
  font-family: 'JetBrains Mono';
  src: url('/fonts/JetBrainsMono-Regular.woff2') format('woff2');
}
```

### 3. Import the generated CSS

Include the Panda CSS output in your entry point:

```tsx
import './styled-system/styles.css';
```

## Architecture

```
@axionic/ui
  src/
    preset/            Panda CSS preset (tokens, text styles, keyframes, semantic tokens)
      colors/          12-step Radix color palettes (teal accent, sand neutral, wheat)
    theme/
      recipes/         local Park UI recipe overrides and Axionic variants
    components/
      ui/              shared primitives exported from `@axionic/ui/primitives`
      patterns/        shared product surfaces exported from `@axionic/ui/patterns`
      layout/          shared layout scaffolding exported from `@axionic/ui/layout`
      forms/           shared form helpers exported from `@axionic/ui/forms`
  styled-system/       Generated by `panda codegen` (tokens, css, recipes, jsx)
```

### Key Technologies

- **Panda CSS**: Build-time CSS-in-JS with typed tokens, atomic CSS output, zero runtime
- **Park UI**: Pre-styled component library built on Ark UI + Panda CSS (equivalent of shadcn for Panda CSS)
- **Ark UI**: Headless, accessible React component primitives (equivalent of Radix UI for Panda CSS)

### Color System

Uses the 12-step Radix color scale convention:

| Steps | Usage |
|-------|-------|
| 1-2 | App/page backgrounds |
| 3-5 | Component/interactive backgrounds |
| 6-8 | Borders and separators |
| 9-10 | Solid backgrounds (buttons, badges) |
| 11-12 | Text and high-contrast |
| a1-a12 | Alpha/transparent variants |

Three palettes defined:
- **Teal** (accent): Derived from Rich Black (#003e44). Used as the primary `colorPalette`.
- **Sand** (neutral/gray): Warm gray scale. Registered as `gray` for Park UI semantic token compatibility.
- **Wheat** (secondary accent): Warm gold scale for CTAs and highlights.

## Import Paths

| Path | Contents |
|------|----------|
| `@axionic/ui` | Root exports: `cn`, `cx` utilities |
| `@axionic/ui/preset` | `createAxionicPreset()` for panda.config.ts |
| `@axionic/ui/primitives` | Shared primitives from `src/components/ui` (Button, Dialog, Select, FileUpload, Tabs, etc.) |
| `@axionic/ui/patterns` | Shared product surfaces from `src/components/patterns` (WorkspacePage, SectionPanel, SlideOver, SelectionList, StatusBanner, ActivityTable, ModelCard, etc.) |
| `@axionic/ui/layout` | Section, DarkSection, SplitSection, PageHeader |
| `@axionic/ui/forms` | FormField, FormMessage, FormAlert |
| `@axionic/ui/utils` | `cx()` and `cn()` class merge utilities |

## Shared Component Catalog

Use the highest-level shared surface that already matches the page:

- Start with `@axionic/ui/patterns` for page shells, panels, dialogs, pickers, list toolbars, status blocks, and selection flows.
- Use `@axionic/ui/forms` when you need consistent label, helper text, and error wiring around a control.
- Drop to `@axionic/ui/primitives` when you need Ark-style composition or a new layout that is not already captured by a pattern.

Full references:

- Package-local catalog: [`COMPONENT_CATALOG.md`](./COMPONENT_CATALOG.md)
- Cross-repo handbook copy: `~/repos/axionic/dev-docs/frontend/axionic-ui-component-catalog.md`

### Shared Product Surfaces At A Glance

| Need | Start with |
| --- | --- |
| Auth pages | `AuthShell` |
| Authenticated route framing | `WorkspacePage`, `TopToolbar`, `SidebarNav`, `SecondaryNav`, `SettingsSectionNav` |
| Collection and library pages | `CollectionPageHeader`, `ListToolbar`, `SelectionToolbar`, `ActivityTable`, `ResourceList` |
| Shared summary cards | `ModelCard`, `EntityCard`, `StatCard`, `ActionCard` |
| Section and inspector surfaces | `SectionPanel`, `FormSection`, `DetailPanel`, `InsetPanel`, `HeroPanel`, `SupportPanel`, `UtilityPanel` |
| Modal and drawer workflows | `ConfirmDialog`, `DetailDialog`, `FormDialog`, `SlideOver`, `SearchPickerDialog` |
| Selection and configuration flows | `PickerField`, `SelectionList`, `OptionRow`, `ChoiceSegment`, `ValueField`, `SecretField`, `NumberField`, `ValueSlider`, `AmountSelector` |
| Status and placeholder content | `StatusBanner`, `StatusState`, `EmptyState`, `GuidedTourCard`, `DocsHint` |

### Primitive Families

| Family | Notable exports | Notes |
| --- | --- | --- |
| Typography and display | `Heading`, `Text`, `Span`, `Link`, `Badge`, `Code`, `Kbd`, `DisplayValue`, `Image` | `DisplayValue` is the main convenience helper for empty-value fallbacks. |
| Actions and feedback | `Button`, `IconButton`, `CloseButton`, `Alert`, `Avatar`, `Loader`, `Spinner`, `Skeleton`, `Progress`, `Toaster` | `Button` adds Axionic loading props and brand variants. |
| Overlays and disclosure | `Dialog`, `Drawer`, `Popover`, `Tooltip`, `Menu`, `Accordion`, `Tabs`, `Card`, `HoverCard`, `Collapsible` | Prefer matching patterns like `FormDialog` or `SlideOver` before rebuilding shared shells on top of these. |
| Inputs and selection | `Field`, `Fieldset`, `Input`, `Textarea`, `Checkbox`, `Switch`, `Select`, `Combobox`, `NumberInput`, `DatePicker`, `ColorPicker`, `FileUpload`, `TagsInput`, `Slider`, `SegmentGroup`, `ToggleGroup` | Most of these keep Ark UI naming conventions (`Root`, `Trigger`, `Content`, `Item`, `Label`, `Control`). |
| Data and structure | `Table`, `Pagination`, `ScrollArea`, `Carousel`, `Splitter`, `Breadcrumb`, `Clipboard` | Use the primitive only when `ActivityTable`, `ResourceList`, or other patterns are too opinionated. |

### Current Axionic Additions Worth Knowing

- `Button` adds `loading`, `loadingText`, `spinner`, and `spinnerPlacement`, plus Axionic variants such as `brand`, `wheat`, `outline-brand`, `light`, `ghost-dark`, `danger`, `pill`, and `pill-active`.
- `Card.Root` adds shared `hover`, `dashed`, `accent`, and `gradient` recipe variants.
- `Dialog.Root` adds Axionic `accent` alongside the usual size, placement, and motion props.
- `Tooltip` is intentionally simpler than the raw Ark primitive: pass `content` and a single child trigger, then opt into `showArrow` or `portalled` as needed.
- `FileUpload`, `Pagination`, `SegmentGroup`, `Slider`, `TagsInput`, and `RatingGroup` expose convenience helpers such as `List`, `Items`, `Marks`, and `Thumbs` so common wiring stays terse.

### Layout Entry Point

`@axionic/ui/layout` is still separate from this catalog. Use it for outer application/page scaffolding such as `Section`, `DarkSection`, `SplitSection`, and `PageHeader`.

## Design Tokens

The preset registers tokens through Panda CSS's theme system. All tokens are available as typed utility props.

### Semantic Color Tokens

Park UI provides semantic tokens that reference the gray/accent scales:

| Token | Description |
|-------|-------------|
| `bg.canvas` | Page background (gray.1) |
| `bg.default` | Card/surface background (gray.2) |
| `bg.subtle` | Subtle background (gray.3) |
| `bg.muted` | Muted background (gray.4) |
| `fg.default` | Primary text (gray.12) |
| `fg.muted` | Secondary text (gray.11) |
| `fg.subtle` | Tertiary text (gray.10) |
| `border.default` | Default border (gray.7) |
| `border.muted` | Subtle border (gray.6) |
| `colorPalette.*` | Current accent color (teal by default) |

Axionic also exposes app-level semantic tokens for product surfaces and accents, including `app.surface`, `app.surface.muted`, `app.accent`, and the wheat-backed `app.accentAlt.*` set used for neutral button hover emphasis.

### Typography

Font families:
- **body**: Graphik (via `fonts.body`)
- **display**: Satoshi (via `fonts.display`)
- **mono**: JetBrains Mono (via `fonts.mono`)

Text styles: `display`, `h1`, `h2`, `h3`, `body`, `small`, `caption`, `label`, `code`

## Development

```bash
bun install          # Install dependencies
bun run codegen      # Generate styled-system/ (panda codegen)
bun run build        # Full build: codegen + JS bundle + type declarations
bun run typecheck    # Type-check without emitting (tsc --noEmit)
bun run lint         # Lint with Biome
bun run lint:fix     # Auto-fix lint and formatting issues
bun run format       # Format with Biome
```

**Important:** Both `dist/` and `styled-system/` are committed to the repository. Consumers install this as a git dependency and depend on pre-built output. After any source changes, run `bun run build` and commit the updated `dist/` and `styled-system/` directories.

### Adding New Components

Park UI components are scaffolded using the Park UI CLI:

```bash
# Add a single component
bunx @park-ui/cli add <component-name>

# Add all components
bunx @park-ui/cli add --all
```

The CLI reads `components.json` for path aliases and configuration. Components are placed in `src/components/ui/`. After adding:

1. Export the new component from `src/components/ui/index.ts`
2. If the CLI generates a recipe file in `src/theme/recipes/`, export it from `src/theme/recipes/index.ts` (add to `recipes` or `slotRecipes` as appropriate)
3. Run `bun run build` to regenerate `styled-system/` and `dist/`
4. Commit the updated `dist/` and `styled-system/` directories

### Adding Custom Recipes

To add a custom recipe variant (e.g., a new Button variant):

1. Edit the recipe in `src/theme/recipes/` (component-level recipes that replace Park UI defaults)
2. Component recipes replace the Park UI defaults (defined at the config level)
3. Run `bun run build` to regenerate

### Project Structure Conventions

- **Preset** (`src/preset/`): Brand tokens, global CSS, custom recipes that extend Park UI defaults. These are distributed as the `createAxionicPreset()` function for consumer panda configs.
- **Theme recipes** (`src/theme/recipes/`): Park UI v1 slot recipes. These are local copies of Park UI's component styling, allowing full customization. Keyed in `panda.config.ts` to override the preset's built-in recipes.
- **Components** (`src/components/ui/`): React components that compose Ark UI primitives with Panda CSS recipes. Generated by Park UI CLI, use `createStyleContext` for compound components.
- **Patterns/Layout/Forms**: Custom components using `css()` and `cx()` from `styled-system/css`. Not generated by CLI.

### Build Pipeline

The build uses Bun's native bundler + tsc for type declarations:

1. `panda codegen` generates `styled-system/` from the Panda config + recipes
2. `Bun.build()` bundles each entry point as ESM with externalized peer deps
3. `tsc --emitDeclarationOnly` generates `.d.ts` files
4. A post-codegen patch fixes known Panda CSS codegen issues (reserved word exports)

### Known Issues

- **`styled-system/recipes/switch.d.ts`**: Panda CSS generates `export declare const switch` which is a JS reserved word. The build script patches this automatically.
- **Preset/config recipe overlap**: The v0.43 Park UI preset includes built-in recipes that may conflict with local v1 recipe files. The build script patches extended slot types (e.g., switch 'indicator' slot) automatically.
